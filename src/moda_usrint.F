	MODULE MODA_USRINT

#ifndef MAXSS_H
#define MAXSS_H
	  USE MODV_MAXSS
#endif

#ifndef NFILES_H
#define NFILES_H
	  USE MODV_NFILES
#endif

#ifdef DYNAMIC_ALLOCATION
	  INTEGER, TARGET, ALLOCATABLE :: NVAL(:)
	  INTEGER, TARGET, ALLOCATABLE :: INV(:,:)
	  INTEGER, TARGET, ALLOCATABLE :: NRFELM(:,:)
	  REAL*8, TARGET, ALLOCATABLE :: VAL(:,:)
#else
	  INTEGER, TARGET :: NVAL(NFILES)
	  INTEGER, TARGET :: INV(MAXSS,NFILES)
	  INTEGER, TARGET :: NRFELM(MAXSS,NFILES)
	  REAL*8, TARGET :: VAL(MAXSS,NFILES)
#endif

	END MODULE
